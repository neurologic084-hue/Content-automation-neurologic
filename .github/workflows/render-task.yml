# Free render host. The Next.js app (on Vercel) fires a repository_dispatch with
# a task payload; this runner checks out the repo and runs worker/run-task.ts to
# completion. State flows through Supabase + R2 only, exactly like the old Vercel
# Sandbox path — the runner is disposable. Ubuntu runners have a modern glibc and
# ffmpeg, so none of the Amazon-Linux glibc patching is needed here.
name: Render task

on:
  repository_dispatch:
    types: [render-task]

jobs:
  render:
    runs-on: ubuntu-latest
    # Outermost of the three render timers; the other two live in
    # motion-renderer (the delayRender budget, which must outlast the WHOLE
    # render rather than just page load, and the subprocess hard kill). This one
    # must sit ABOVE both or it becomes the silent killer itself — which is
    # exactly how healthy long renders were dying. 4K sources are now downscaled
    # before render, so this is a runaway guard, not a working limit.
    timeout-minutes: 120
    steps:
      # ref lets a dispatch render from a branch instead of main — used for
      # diagnostic renders (e.g. bisecting a regression on the real runner
      # without touching main). Empty/absent = default branch, as before.
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.client_payload.ref || '' }}

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      # Only the Remotion render (v4-v6) needs the remotion package tree + a
      # browser. The Submagic + ffmpeg tasks don't, so skip this for them.
      - name: Install Remotion + browser
        if: github.event.client_payload.task == 'render-variant'
        run: |
          cd remotion
          npm ci --no-audit --no-fund
          npx remotion browser ensure
          cd ..

      - name: Run task
        env:
          # The task descriptor ({task, jobId, ...}). Passed via env (not inlined
          # into the script) so its contents can never be shell-interpreted.
          PAYLOAD: ${{ toJSON(github.event.client_payload) }}
          RENDERS_DIR: /tmp/renders
          # Test renders only: dispatch with client_payload.skipSubmagicClean="1"
          # to skip Submagic Clean Audio (a paid project per variant) and use the
          # ElevenLabs fallback instead. Absent on real client renders.
          SKIP_SUBMAGIC_CLEAN: ${{ github.event.client_payload.skipSubmagicClean }}
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          GEMINI_MODEL: ${{ secrets.GEMINI_MODEL }}
          SUBMAGIC_API_KEY: ${{ secrets.SUBMAGIC_API_KEY }}
          AUPHONIC_API_TOKEN: ${{ secrets.AUPHONIC_API_TOKEN }}
          ELEVENLABS_API_KEY: ${{ secrets.ELEVENLABS_API_KEY }}
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
          R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_PUBLIC_URL: ${{ secrets.R2_PUBLIC_URL }}
        run: |
          echo "Task: $(echo "$PAYLOAD" | head -c 200)"
          node --import tsx worker/run-task.ts "$(printf %s "$PAYLOAD" | base64 -w0)"
